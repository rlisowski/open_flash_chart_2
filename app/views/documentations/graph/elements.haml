%strong
  Graph.elements
%br
%br
This property is an array with any graph you want to generate on chart.
%br
To set this property use
%strong
  add_element( element )
method on Graph class object. There is also an alias fot this method
%strong
  <<
\.
%br
.tip.example
  .chart
    = @simple_chart_1
  .code
    - code = ''
    - code << "title = OFC2::Title.new( :text => 'elements')\n"
    - code << "line_dot = OFC2::Line.new( :values => [9,8,7,6,5,4,3,2,1,12] )\n"
    - code << "chart = OFC2::Graph.new(:title => title)\n"
    - code << "chart << line_dot\n"
    .code_example
      %pre{:class => "brush: ruby;"}=code
%br
You can also simply set this using array.
%br
.tip.example
  .chart
    = @simple_chart_2
  .code
    - code = "title = OFC2::Title.new( :text => 'elements')\n"
    - code << "line_dot = OFC2::Line.new( :values => [9,8,7,6,5,4,3,2,1,12] )\n"
    - code << "chart = OFC2::Graph.new(:title => title, :elements => [line_dot])\n"
    .code_example
      %pre{:class => "brush: ruby;"}=code
